"""
Auriga Data Factory
===================
Research laboratory for the Auriga spatial intelligence ecosystem.

Subsystems:
  factory.ingestion    — normalize raw data to canonical schema
  factory.validation   — 7-layer quality validation pipeline
  factory.generation   — mathematically derived synthetic data
  factory.versioning   — dataset lifecycle and immutability management
  factory.coverage     — gap detection and collection recommendations
  factory.reporting    — human-readable validation and dataset reports
  factory.export       — packaging approved datasets for downstream consumers
"""

__version__ = "1.0.0"
__schema_version__ = "canonical_v1"
